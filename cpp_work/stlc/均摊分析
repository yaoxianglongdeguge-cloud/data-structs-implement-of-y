
/*

--------------------------------------------------------------------------------------------------------------------------------
伸展树
均摊分析

  势能法，核心思想在于，对于单次操作（对伸展树来说是一次完整展开），代价可能很高也可能很低，或者比较平稳，那么设定一个势能函数，
作用就是，在i-1状态经过第i次操作，到第i状态时，假设真实代价为c，位势函数的变化为D(i)-D(i-1),那么设定c*=c+D(i)-D(i-1)，并且，（#为累加符）
#c*=#c+#（D(i)-D(i-1)）=#c+D(n)-D(0)，我们希望的是求#c的界，再除以次数，得出每一次操作的均摊代价，但事实是，由于一列操作是随机的，因此对于
之前的真实花费累计我们是不知道的，并且难以确定一个界，因为上下差距比较大，但如果我们把每次操作都用一个函数平衡一下，代价高就吸收代价，代价低就释放
，那显然，得出来的结果就是c*，c*大致的界是能确定的，而把c*累计起来，就是#c*=#c+D(n)-D(0)，也就是说，
我们可以知道一次操作的代价的界，但这个界太大（要记住，不是说一次操作的代价算不出来，而是无法用来放缩得到一个紧的界）
如果我们尝试用这个界去放缩，算累计的界，就会导致过于松弛，而c*界就小得多，用这个放缩累计就能得出一个比较紧的界。

  那么问题就是这个位势函数一次操作的变化需要与一次操作代价的高低有较强的相关性（但未必是一一对应的映射关系），这样才能让c*的界更窄

  但问题是，#c*可以得出一个比较紧的界，但这和#c什么关系呢，如果D(n)-D(0)是负值，那#c大于#c*，是不是又没法确定界了，但这个不需要担心，
由于我们的累加是趋向无穷的，也就是非常大，那么只要保证D(n)是一个低阶的，那么#c=#c*-D（n）+D（0），#c的界的阶肯定是由#c*决定的。

  所以核心问题是，我要求大量次操作的真实代价的累计，但我不可能去一个个算，而只能通过单次操作的真实代价放缩，但单次操作真实代价的振幅太大，
会导致放缩的界不够紧，那么我引入一个位势函数，对每一次操作添加上这个位势函数的变化量，从而得到一个新函数c*，c*的振幅小，通过放缩算出的界更紧，
而由于D（n）的低阶性，#c也可以通过#c*得出，这就是势能法的内涵。

-----------------------------------------------------------------------------------------------------------------------------------
  接下来就这个题分析，势能函数选取的是#logS(i)，为什么选取这个函数
  首先，相关性分析：一次操作代价高低与什么相关？对某个节点x展开，深度大自然真实代价就高。而对于这个函数，由于我们考虑的展开都是在节点数不变的树里，
那么函数如果大，就意味着结构上更偏向链，也就是说这个函数实际上与树的平衡度是较强相关的，而代价高的展开，大多数是在一颗不平衡的树的深处节点开始的，
展开完成后，对树平衡性贡献大，因此函数结束减开始状态是负的，就可以调和真实代价，而反过来，如果是代价比较小的操作，往往和一棵平衡树走向不平衡相关，
因此，也进行了调和。
其次，数量级分析，就是说，S（i）累乘和#logS（i）趋势相同，为什么不用前者，因为前者数量级太大，已经超出了一次操作真实代价的范围，导致，大代价一减，
或者小代价一加不是趋中而是直接飞出界了，反而让界更不紧了，因此不行。
最后，阶分析，D（n），由于节点数是确定的，因此无论树结构怎么样，#logS（i）界是确定的，一个常数界，因此可以。

  所以经过这几点分析，这个函数大概是可以的，至于是不是真的可以，还要去算，看看结果是否符合猜想。
  -----------------------------------------------------------------------------------------------------------------------------------
  那么深入定量分析，也就是算c*的界，由于我们上面经过分析已经确定了一个等式c*=c+D(i)-D(i-1)，之后就是单纯的数学手法，从而算出来了c*的界，没问题。

*/